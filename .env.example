# PayMob (DO NOT commit real secret keys to the repo)
# Copy this file to .env.local or set these variables in Vercel/host provider.

# Use PayMob test keys in development; rotate keys if you accidentally exposed them.
PAYMOB_API_KEY=egysktest_YOUR_SECRET_KEY
PAYMOB_INTEGRATION_ID=YOUR_INTEGRATION_ID
PAYMOB_IFRAME_ID=YOUR_IFRAME_ID  # optional: iframe id to embed the payment page
PAYMOB_CALLBACK_URL=https://your-site.com/payment-callback

# Other existing envs used by the project
NEXT_PUBLIC_ADMIN_PASSWORD=your_admin_password
GOOGLE_SHEET_WEBHOOK=https://script.google.com/macros/s/XXX/exec
ADMIN_SHEET_URL=https://docs.google.com/spreadsheets/d/XXX

# NOTE: If you accidentally pasted real secret keys into the repo or chat, rotate them immediately
# and revoke the old keys in the provider dashboard.